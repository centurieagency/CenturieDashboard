# Centurie

Next.js (App Router) · TypeScript strict · NextAuth (Credentials + Google) · Zod

## Démarrage

```bash
npm install
cp .env.example .env.local   # puis remplir les variables
npm run dev
```
